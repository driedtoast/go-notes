package commands

// TODO
// - Define an interface for commands
// - add commands all in separate files
// - create a dynamic calling model

type Command interface {
	// DO functions here
	// run(dictionary, db_connection?, writer)
}

// commands: list notes, search notes, tag notes, create notes, create schema file for db setup, setup
